// const express = require("express");
// const cartRoutes = express.Router()
// const Cart = require('../../controller/checkout/cartController')
// const { verifyToken } = require('../../middleware/verifyToken')

// cartRoutes.get('/:cart', verifyToken(), Cart.getCartItems)
// cartRoutes.post('/:cart', Cart.editCartItems)
// // cartRoutes.delete('/:id', Cart.deleteCartItems)

// module.exports = cartRoutes